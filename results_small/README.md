# Compact Results

This folder contains small CSV summaries only. Large raw artifacts are excluded from Git.

Important result groups:

- `surrogate_gradient_opt_top5_move025_risk035_validation_80seeds/`
  - latest 5-design optimized validation
  - current best: `surrogate_opt_from_288`

- `feature_v2compact_ablation_compare.csv`
  - rank-only feature variant comparison

- `edgeset_force_curve_opt_area_context_fullcombo_h512_metrics_summary.csv`
  - gradient-ready surrogate metrics, if present

The full raw artifacts remain local and are listed in `../data_manifests/local_artifact_manifest.md`.
